export enum Environments {
    staging = 'staging',
    production = 'production'
}

export enum ServerVariablesValues {
    Token = 'token',
    Environment = 'environment',
    AppVersion = 'appVersion',
    Elections = 'elections',
    Assets = 'assets',
    AssetPrefix = 'assetPrefix',
    ApiHost = 'apiHost'
}

export enum AnalyticsEvent {
    Pageview = 'pageview',
    Custom = 'custom'
}
